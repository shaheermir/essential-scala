/**
 * A block is a sequence of expressions or declara􀦞ons surrounded by braces. A
 * block is also an expression: it executes each of its sub-expressions in order
 * and returns the value of the last expression.
 */

{
  1;
  2;
  3;
  4
}


def name: String = {
  val title = "Professor"
  val name = "Funkenstein"
  title + " " + name
}